# Incoming Webhooks integration URL. See https://my.slack.com/services/new/incoming-webhook
webhook_url: ""
# The user's API authentication token, only used for file uploads. See https://api.slack.com/#auth
upload_token: ""
# Default channel to post messages. '#' is prepended, if it doesn't start with '#' or '@'.
channel: ""
# Temporary file is created in this directory.
tmp_dir: "/tmp"
# Default username to post messages.
username: "slacktee"
# Default emoji or a direct url to an image to post messages. You don't have to wrap emoji with ':'. See http://www.emoji-cheat-sheet.com.
icon: "ghost"
attachment: ""
